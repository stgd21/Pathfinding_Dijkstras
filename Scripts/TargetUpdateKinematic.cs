using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TargetUpdateKinematic : MonoBehaviour
{
    public Kinematic thisKinematic;
    // Start is called before the first frame update
    void Start()
    {
        thisKinematic = new Kinematic();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
